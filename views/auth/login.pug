extends ../layouts/main-layout.pug

block content
  h1(class="text-3xl font-bold kenpo-font mb-6 text-green-700") Login to KenpoKicks

  if emailChange
    p.text-blue-600 Your email was updated. Please verify your new email to continue.

  if passwordChange
    p.text-blue-600 Your password was changed successfully. Please log in again.

  if errorMessage
    p(class="bg-red-100 text-red-700 px-4 py-2 mb-4 rounded-lg shadow") #{errorMessage}

  form(action="/auth/login" method="POST" class="flex flex-col space-y-4 max-w-md mx-auto text-left")
    label(for="login" class="block font-semibold") Email or Username
    input(type="text" name="login" id="login" required class="w-full border border-gray-300 px-3 py-2 rounded")

    label(for="password" class="block font-semibold") Password
    input(type="password" name="password" id="password" required class="w-full border border-gray-300 px-3 py-2 rounded")

    button(type="submit" class="bg-red-800 hover:bg-red-700 text-white font-bold py-2 px-4 rounded shadow transition") Log In

  p(class="mt-6 text-center")
    | Don’t have an account?
    a(href="/auth/signup" class="text-red-700 hover:underline")  Sign up here
